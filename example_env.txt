# =============================================================================
# STENCIL FLOW - COMPREHENSIVE ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains all environment variables for both frontend and backend
# Copy this file to .env and fill in your actual values

# =============================================================================
# FRONTEND CONFIGURATION (Vite/React)
# =============================================================================

# Frontend Build Environment
NODE_ENV=development

# API Server URL (used by frontend to connect to backend)
VITE_SERVER_URL=http://localhost:8000

# =============================================================================
# BACKEND CONFIGURATION (Django)
# =============================================================================

# Django Core Settings
SECRET_KEY=your-super-secret-django-key-change-this-in-production
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1,0.0.0.0

# Database Configuration
DB_NAME=stencil_flow
DB_USER=postgres
DB_PASSWORD=your-database-password
DB_HOST=localhost
DB_PORT=5432

# JWT Authentication
JWT_SECRET_KEY=your-jwt-secret-key-change-this-in-production

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000,http://127.0.0.1:3000
CORS_ALLOW_ALL_ORIGINS=True

# Application URLs
SITE_URL=http://localhost:3000
SERVER_URL=http://localhost:8000

# =============================================================================
# GITHUB OAUTH INTEGRATION
# =============================================================================

# GitHub OAuth App Credentials
GITHUB_CLIENT_ID=your-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-client-secret
GITHUB_CALLBACK_URL=http://localhost:8000/api/auth/github/callback/

# =============================================================================
# N8N INTEGRATION
# =============================================================================

# Default N8N Instance (for master workspace)
DEFAULT_N8N_INSTANCE_URL=https://your-n8n-instance.com
DEFAULT_N8N_API_KEY=your-n8n-api-key

# =============================================================================
# AI SERVICES
# =============================================================================

# OpenAI API (for Mermaid chart generation and AI features)
OPENAI_API_KEY=sk-your-openai-api-key-here

# =============================================================================
# CLOUD STORAGE (AWS) - Optional
# =============================================================================

# AWS S3 Configuration (optional - for file storage)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-s3-bucket-name
AWS_REGION=us-east-1

# =============================================================================
# DOCKER COMPOSE OVERRIDES
# =============================================================================
# These are used by Docker Compose for container orchestration

# PostgreSQL Container
POSTGRES_DB=stencil_flow
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres_password

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
# 
# 1. Copy this file to .env and fill in your actual values
# 2. Replace all placeholder values with your actual credentials
# 3. Never commit .env files with real credentials to version control
# 4. For production, use different values for DEBUG, ALLOWED_HOSTS, etc.
# 5. Generate strong random keys for SECRET_KEY and JWT_SECRET_KEY
# 
# Required for basic functionality:
# - DATABASE_URL or DB_* variables
# - SECRET_KEY and JWT_SECRET_KEY
# - GITHUB_CLIENT_ID and GITHUB_CLIENT_SECRET (for OAuth)
#
# Optional but recommended:
# - OPENAI_API_KEY (for AI features)
# - AWS credentials (for file storage)
# - DEFAULT_N8N_* (for N8N integration)
#
# =============================================================================

