# =============================================================================
# STENCIL FLOW - PRODUCTION ENVIRONMENT CONFIGURATION
# =============================================================================
# This file contains production environment variables for both frontend and backend
# Copy this file to .env.production and fill in your actual production values

# =============================================================================
# FRONTEND CONFIGURATION (Vite/React)
# =============================================================================

# Frontend Build Environment
NODE_ENV=production

# API Server URL (used by frontend to connect to backend)
VITE_SERVER_URL=https://your-production-domain.com

# =============================================================================
# BACKEND CONFIGURATION (Django)
# =============================================================================

# Django Core Settings
SECRET_KEY=your-super-secure-production-secret-key-min-50-chars
DEBUG=False
ALLOWED_HOSTS=your-production-domain.com,www.your-production-domain.com,your-alb-dns-name.amazonaws.com

# Database Configuration (Production - typically RDS)
DB_NAME=stencil_flow_prod
DB_USER=postgres
DB_PASSWORD=your-very-secure-production-database-password
DB_HOST=your-rds-endpoint.region.rds.amazonaws.com
DB_PORT=5432

# JWT Authentication
JWT_SECRET_KEY=your-super-secure-jwt-secret-key-different-from-django-secret

# CORS Configuration
CORS_ALLOWED_ORIGINS=https://your-production-domain.com,https://www.your-production-domain.com
CORS_ALLOW_ALL_ORIGINS=False

# Application URLs
SITE_URL=https://your-production-domain.com
SERVER_URL=https://your-production-domain.com

# =============================================================================
# GITHUB OAUTH INTEGRATION
# =============================================================================

# GitHub OAuth App Credentials (Production App)
GITHUB_CLIENT_ID=your-production-github-oauth-client-id
GITHUB_CLIENT_SECRET=your-production-github-oauth-client-secret
GITHUB_CALLBACK_URL=https://your-production-domain.com/api/auth/github/callback/

# =============================================================================
# N8N INTEGRATION
# =============================================================================

# Default N8N Instance (Production)
DEFAULT_N8N_INSTANCE_URL=https://your-production-n8n-instance.com
DEFAULT_N8N_API_KEY=your-production-n8n-api-key

# =============================================================================
# AI SERVICES
# =============================================================================

# OpenAI API (Production API Key)
OPENAI_API_KEY=sk-your-production-openai-api-key-here

# =============================================================================
# CLOUD STORAGE (AWS) - Required for Production
# =============================================================================

# AWS S3 Configuration
AWS_ACCESS_KEY_ID=your-production-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-production-aws-secret-access-key
AWS_STORAGE_BUCKET_NAME=your-production-s3-bucket-name
AWS_REGION=us-east-1

# =============================================================================
# DOCKER COMPOSE OVERRIDES (Production)
# =============================================================================

# PostgreSQL Container (if using Docker for database)
POSTGRES_DB=stencil_flow_prod
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-very-secure-production-database-password

# =============================================================================
# PRODUCTION SECURITY NOTES
# =============================================================================
# 
# CRITICAL SECURITY REQUIREMENTS:
# 1. Use strong, unique passwords for all services (min 32 chars)
# 2. Set DEBUG=False in production
# 3. Use HTTPS for all URLs
# 4. Restrict ALLOWED_HOSTS to specific domains
# 5. Set CORS_ALLOW_ALL_ORIGINS=False
# 6. Use different OAuth apps for production
# 7. Store secrets in AWS Secrets Manager or similar
# 8. Enable database encryption at rest
# 9. Use IAM roles instead of access keys when possible
# 10. Regular security updates and monitoring
#
# AWS DEPLOYMENT CHECKLIST:
# - RDS PostgreSQL instance with encryption
# - ECS/EC2 with proper security groups
# - ALB with SSL certificate
# - CloudFront for static assets
# - Route 53 for DNS
# - AWS Secrets Manager for sensitive data
# - CloudWatch for monitoring
#
# =============================================================================

